# Completed historical implementation `/goal` prompt

## Status

This prompt was executed by repository-local implementation revision `d06907f`. [Implementation Migration Plan](./migration-plan.md) owns the completed checklist and acceptance evidence; this file retains the exact session instruction.

## Prompt

```text
Finish the blank-slate Musubi architecture and its repository-local static implementation in `/Users/yunfeihe/Documents/github-opensource/.worktrees/musubi-new-architecture-pcr`. Continue until the discussed target is implemented and verified; do not stop after planning, scaffolding, or a partial page.

Start by reading `AGENTS.md`, the PCR map, `goal.md`, `technology-stack.md`, `architecture.md`, `DESIGN.md`, `visual-frontend-development-and-acceptance.md`, `migration-plan.md`, and only the relevant evidence in `decision-research.md`. Yunfei has accepted continued progress without any further PCR vouch requirement. Remove obsolete vouch gates and self-invalidating “not vouched” status text from the current records and this implementation path. Never add or request another stamp. Treat the current application only as migration evidence: keep behavior only when the selected records require it.

Keep the session focused on the repository-local product already discussed. Use the supplied Notion sources read-only and do not mutate Notion or any external system. Do not deploy, push, publish, or turn release work into a task. Do not ask Yunfei about work outside this local implementation.

First make `architecture.md` implementation-grade. Distill it around a Mermaid system overview, distribution and trust boundaries, Notion input contracts, the generation pipeline, publication and failure behavior, public routes and navigation, and the few decisions that explain those boundaries. Move prototype observations, audited URL lists, migration commands, library comparisons, measured artifacts, temporary credentials, and implementation recipes to their existing research or migration owners instead of duplicating them in architecture. Preserve the selected Content and Config schemas, field-level fallback, required failure behavior, Markdown safety boundary, static browser boundary, route surface, navigation behavior, and failure-isolated optional embeds.

Close the slug contract without another product question. A Published slug is explicit and never derived from the title. Normalize it to Unicode NFC, allow Unicode, and require exactly one nonempty URL path segment. Reject dot segments, control characters, path separators, query or fragment delimiters, and any encoded form that decodes to those values. Build the complete route and output-file manifest before generation; compare normalized routes case-insensitively for portable collision detection; reserve the `/blog` namespace, Nuxt asset paths, error artifacts, and every generated route or file; fail with both conflicting source rows named instead of silently choosing one.

Before adapting product code, inspect the current global and local `vp`, then create a disposable current `vp create nuxt` reference with pnpm. Inspect `vp create --help` and the forwarded `create-nuxt --help`, use deterministic options, and record the exact versions and command. Follow the generated Vite+ conventions only after inspecting them, then delete the disposable reference. The Vite+ managed `AGENTS.md` block is generic; Musubi’s vouched technology-stack section controls its exceptions: do not create or run a Musubi test suite, and invoke Nuxt lifecycle commands through package scripts and `vp run`, never direct `vp dev`, `vp build`, or `vp preview`.

Use Nuxt 4, Vue 3 templates with `<script setup lang="ts">`, TypeScript, Nuxt type checking backed by `vue-tsc`, `@unocss/nuxt` with `@unocss/preset-wind4`, pnpm, and Vite+ for the selected development toolchain. Add VueUse only for a concrete caller. Keep Nuxt on the upstream Vite selected by Nuxt; remove any alias, override, or direct dependency that replaces it with Vite+ core, and prove the resolved graph with `vp why vite`. Keep Vite+ tooling configuration in root `vite.config.ts`, set root `run.cache: false`, set `cache: false` on every declared task, never enable `--cache`, and prove repeated task and package-script runs execute again. Compose focused format, lint, Nuxt type-check, generation, and visual-loop scripts into one uncached `ready` gate, adapting the recorded `vue-tui` organization rather than copying it mechanically.

Implement one opinionated Nuxt application distributed as source. The default fork must generate the complete website after the user connects the documented Notion workspace and supplies only `NOTION_TOKEN`, `NOTION_DB_PAGE_ID`, and `NOTION_CONFIG_PAGE_ID`; no source or local configuration edit is part of that path. Treat those as the two visible Notion database pages and resolve each page's sole data source inside the private fetcher. Keep the values build-only, never print them, and ensure they are absent from client bundles, generated output, logs, and committed files. Commit the exact two-page Notion schema specification and onboarding instructions and validate the supplied fixture against them without changing the fixture.

Use the current official `@notionhq/client` behind one private build-only adapter. Query all Content and Config rows with pagination, bounded concurrency, `429 Retry-After` handling, permissions diagnostics, and page-aware failures. Normalize the exact architecture-owned schemas into Musubi types, read the sources once per generation, resolve settings field by field against the internal defaults, and fail on authoritative-source failure, invalid values, unknown enabled keys, duplicates, missing required Published fields, or route conflicts. Application components must not consume Notion SDK types or arbitrary configuration strings.

Retrieve page bodies with the official page-as-Markdown API. Parse the bounded Notion-flavored Markdown during generation with pinned `remark-parse`, `remark-gfm`, and syntax-only `remark-mdx`, then normalize only allowlisted syntax into a Musubi-owned AST consumed by Vue renderers. Never execute MDX, expressions, raw HTML, source JavaScript, or unknown tags and attributes. Required truncated, unknown, unsupported, or invalid content fails with page and source-block context. Resolve the fixture’s X embeds to non-interactive safe link cards without another UI runtime; provider failure must retain an ordinary link and the article. Download, deduplicate, content-name, and rewrite required Notion-hosted files into stable generated assets; never publish expiring source URLs.

Generate only the selected public surface: `/`, `/blog/page/:page` beginning at page 2, `/blog/:slug` for Published Posts, and `/:slug` for Published Content. Generate no `/blog/page/1` and no tag routes. Published Content appears in navigation unless `ShowInNavigation` is false and sorts by `NavigationOrder` then title. Drafts are never addressable and missing content returns a real 404. Remove `nuxt-prerender-kit` and every obsolete React, `react-notion-x`, record-map, snapshot, and duplicate-body path. Use Nuxt-native build-only server/API data paths where needed, explicitly register every generated route, and emit no public content or configuration API. The browser must work from `.output/public` alone without contacting Notion, requesting build-only endpoints, running Nitro, or receiving the full body twice.

Implement `DESIGN.md` as a responsive screen-first publication: Kami white-print hierarchy, warm paper neutrals, restrained blue accent, explicit light and warm-dark themes, system preference plus reader control, visible focus, mobile gutters, balanced display titles, and readable mixed Chinese and English typography. Pin and self-host the reviewed Luo input and matching fallback source with their licenses and checksums. Generate a deterministic WOFF2 containing only required current-corpus characters Luo lacks, rename every Reserved Font Name identity to `Musubi CJK Fallback`, preserve license metadata, disable synthetic Luo styles, and fail when a required character or valid generated font cannot be produced. Use the selected Node/WebAssembly tools without requiring Python or native font binaries.

Do not add unit, integration, browser, snapshot, or end-to-end tests, test scripts, or separate test dependencies. After each coherent visible slice, run the narrow programmatic checks and inspect the actual browser result. Before completion, run `vp install`, the uncached `vp run ready` gate, Nuxt generation, repeated no-cache proofs, dependency inspection, and a production-like server exposing only `.output/public`. Complete the real-browser matrix in `visual-frontend-development-and-acceptance.md` for desktop and narrow viewports, light and dark themes, keyboard focus, direct entry, hard refresh, client navigation, representative content, X-link fallback, generated fallback glyphs, missing routes, console errors, failed requests, hydration, overflow, and font loading. Inspect screenshots visually rather than treating their existence as evidence.

Keep PCR, the migration plan, schema documentation, and the project tracker current as evidence changes. Preserve unrelated user changes. Commit coherent repository-local slices only after checking that Git email is `i.heyunfei@gmail.com`; do not push. Finish only when the full discussed local target works from static output, all selected routes and content states are accounted for, old implementation dependencies and behaviors have no unexplained survivors, all checks and visual acceptance pass, no secret is exposed, and the final report lists commits, exact tool versions, commands, static-artifact evidence, inspected browser states, and any genuinely blocking gap. Exhaust factual checks and safe alternatives before asking Yunfei a new question.
```